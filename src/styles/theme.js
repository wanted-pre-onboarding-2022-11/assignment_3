const color = {
  black: "#000000",
  gray: "#d9d9d9",
  blue: "#0094ff",
  white: "#FFFFFF",
};

const bgColor = {
  primary: color.black,
  secondary: color.gray,
  tertiary: color.blue,
};

const fontColor = {
  primary: color.white,
  secondary: color.black,
  tertiary: color.white,
};

const theme = {
  color,
  bgColor,
  fontColor,
};

export default theme;
